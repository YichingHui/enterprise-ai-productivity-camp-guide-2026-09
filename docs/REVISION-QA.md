# 截图修订独立 QA 记录

日期：2026-09-05。范围：`fix/annotated-guide-revision` 中学员须知页面的截图修订。本文只记录独立内容/代码审阅与隔离 DOM 模型证据；真实浏览器、正式上线和微信真机证据统一见 `TEST-REPORT.md`，不在此重复维护发布状态。

## 结论

本次审阅未发现阻断性的内容或图片弹窗状态逻辑缺陷。`node frontend/tests/content-check.mjs` 实际通过 **231 项静态验收 + 17 项隔离 DOM 模型验收**。保留 **3 个正式问卷入口、6 个高德入口**，没有向正式问卷写入数据。

## 静态覆盖

- 四项精确文案：点击填写问卷、课程时段安排、学员须知、授课老师；旧文案及“AI管理艺术家”不再出现在正文。
- 讲师截图中的三项身份、两段完整介绍，以及 2023 / 2024 / 2025.04–NOW 三段经历均存在；讲师模块没有遗留旧照片、30+ / 300+ 统计卡。
- 页头引用新白色 RGBA PNG，输出尺寸 2127×599；白底卡移除。讲师图声明 1800×1198，使用 `height:auto` 与 `object-fit:contain`；实际视觉完整性仍需浏览器检查。
- DoubaoWork、WorkBuddy、Codex 三项入口使用已核验官方 URL，保持“无需工具焦虑”“不必提前全部安装完毕”。入口来源与平台限制见 `SOFTWARE-LINKS.md`。
- 两张交通/周边图片有“点击展开”“点击收起”“点击放大”引导；提供同页弹窗和不依赖 JavaScript 的 JPG 原图链接，不再把指南图链接指向新标签 WebP。
- JPG 文件格式正确且均小于 800 KiB；弹窗有加载状态、错误重试、原图降级入口、放大按钮、关闭按钮、可访问标题及操作说明。
- 手机日程标签保留完整原文，并存在不换行及日期第二行布局规则；这是 CSS 契约检查，不是 320 / 390 / 430 px 实际渲染证明。
- 原课程时间、地址、388 元协议价、双早、提前入住日期、老师/酒店电话、地图坐标/POI/网页与 App 参数继续通过既有回归。

## 隔离 DOM 模型实际运行的路径

测试直接执行当前 `frontend/app.js`，使用 Node `vm` 和最小 DOM 模型，不访问网络、不启动浏览器。

1. 第一张 JPG 同页打开，加载中状态与禁用放大。
2. 模拟加载成功后显示图片、清理超时、启用放大。
3. 放大与适应屏幕切换、同步 `aria-pressed`。
4. 关闭后解除正文滚动锁、焦点返回对应入口。
5. 第二张 JPG 替换来源、标题及原图链接。
6. 模拟加载错误后展示重试与原图提示。
7. 重试当前图片、重置 15 秒定时器。
8. 模拟超时提示，随后慢网图片成功返回时恢复显示。
9. 弹窗外点击关闭并返回入口焦点。
10. 无 `showModal` 的浏览器保留普通 JPG 链接，不被 JavaScript 拦截。
11. 原酒店经理二维码弹窗打开、关闭和焦点/滚动恢复回归。

## 命令与边界

实际执行并通过：

```sh
node frontend/tests/content-check.mjs
node --check frontend/app.js
node --check frontend/tests/content-check.mjs
git diff --check
```

本 QA 未使用 CUA、未操作用户浏览器、未提交 Git、未推送或发布；只修改既有内容测试及本记录。未声称通过实际 JPG 网络加载、真实图片解码、手机排版、微信长按识别或 App 唤起。真实交互和部署验收由主 Agent 接续完成。
