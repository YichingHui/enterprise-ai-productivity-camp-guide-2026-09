# 杭州课前须知独立 QA

## 当前状态

- 2026-09-05：无额外依赖的 Node 静态检查已在完整页面、样式、脚本与素材到位后通过；浏览器交互由主任务验证。
- 权限：仅维护 `frontend/tests/content-check.mjs` 与本文件；不修改页面、素材、Git、部署或飞书。
- 检查命令：`node frontend/tests/content-check.mjs`。

## 静态验收范围

- 9 月 19—20 日课程日期、签到和全部时段；两日结束 17:30，首日晚间实操答疑 19:00—21:00。
- 杭州酒店二楼会场；大床和双床协议价 388 元每间每晚、含两份早餐、9 月 18 日提前入住适用、午晚餐自理。
- 酒店经理陈诺 13540620182，意心会谢老师 13594719905；携带电脑及充电器、现场助教协助安装与工具焦虑提示。
- 所有问卷入口均为现有正式表单，预计填写 7—9 分钟；高德 POI 为 `B0FFJTFFJU`，会场坐标为 `120.267222,30.188177`。
- 禁止旧期日期、重庆地点、旧问卷、订房电话、房价、坐标、短链与旧项目 URL 残留。
- HTML 锚点、重复 id、本地资源存在性、图片 alt 与宽高、新窗口链接 noopener。

## 必须由主任务实际验证的范围

- 电脑与手机版面、Logo 完整显示及减少动态效果偏好。
- 二维码点击放大、关闭/返回、微信长按识别；弹窗焦点与滚动恢复。
- 复制成功及不支持剪贴板时的兜底、电话链接、高德目的地与回到页面。
- 问卷跳转、返回，首页/固定栏不能显示无真实依据的已填写状态。
- 发布后的正式入口、图片和关键内容回读；GitHub Pages/微信提示不得承诺已消除。

## 审查记录

- 已审读首版 `index.html`：两日日程、双房型协议价、会场坐标、正式问卷链接、锚点与资源路径结构符合要求；未发现旧期正文残留。
- 酒店经理电话：主任务已从原始长图高清核验为 `13540620182`；测试已锁定该值，并禁止初始 OCR 误读值 `13540260182` 出现在发布页面。
- 问卷卡按批准方案保留“预计 7—9 分钟”；不重复维护题数，不修改正式问卷。初始“17 题”断言已按主任务指示移除。
- 高德采用网页优先 `callnative=0`、保留 App 备选 `callnative=1`。静态验收已支持该设计，不再沿用旧版全部尝试唤起 App 的假设。
- 完整静态检查通过：170 项检查、3 个正式问卷入口、6 个高德入口、10 个图片实例、11 个本地资源。所有资源均为可在 Pages 子目录使用的相对路径。`node --check frontend/app.js` 与静态检查脚本语法检查均通过。
- 已审读 `app.js`、`styles.css` 和 Pages 工作流：复制先用剪贴板 API，失败后尝试旧浏览器复制，再给手动复制文本；二维码使用原始图片与原生 modal dialog，含关闭、Escape、焦点恢复与旧浏览器原图回退；不拦截问卷跳转、不推测填写状态。
- 源码可访问性检查：有页面语言、主标题/地标、跳转主内容入口、可见焦点、dialog 标题及关闭按钮、读屏播报 toast、减少动态效果处理；主要按钮声明最小 44—50px 触控高度。实际尺寸、对比效果及键盘/手机行为不能仅靠源码断言。
- Pages 工作流先执行内容检查，再仅暂存 HTML/CSS/JS/assets 发布，不把 tests 或内部文档直接上传为站点内容。
- 当前源码审查未发现会阻止上线的事实偏差或可访问性缺陷。真实微信首次进入、导航目的地、二维码识别、剪贴板及公网回读仍需主任务验收，未由本次独立 QA 冒称通过。
